# Release

Build releases on native operating systems:

- macOS runner for `.app` and `.dmg`
- Windows runner for `.exe` or `.msi`
- Linux runner for AppImage and optionally `.deb`

Do not cross-build macOS or Windows desktop packages from another OS.
